# Changelog

All notable changes to this repository should be documented here.

## Unreleased

- Added strict FigurePattern models, Markdown-authoritative Repository storage,
  deterministic queries, seeded mock data, renderer adapters, objective
  validation, and the install-free JSON-Envelope CLI.
- Integrated self-evolution updates with Markdown-backed Repository writes and
  strict derived-index rebuilding.
- Documented standalone use without `nature-figure`, explicit bridge use,
  agent compatibility, dependencies, verification, and public-repository
  safety.

## 0.2.0

- Added `memory_score` as a composite retrieval signal.
- Added `success_cases`, `failure_cases`, and `recommendation_rationale`.
- Added `kb:why <id>` recommendation explanation workflow.
- Added style reflections and meta-pattern guidance.
- Reworked `self_evolution_engine.py` with clean ASCII console output.
- Added unit tests for memory scoring and evolution artifacts.

## 0.1.0

- Initial figure analysis workflows.
- Added image analysis, code analysis, KB query, growth report, and integration bridge references.
- Added KB location manager and self-evolution helper script.
